import { submissionDate } from '../../../helpers/submission-date';
import { module, test } from 'qunit';

module('Unit | Helper | submission date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = submissionDate(42);
  assert.ok(result);
});
