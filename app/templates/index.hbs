<h1>Welcome to Message Board</h1>

<ul>
  {{#each model as |question|}}
    <li>{{#link-to 'question' question.id}}{{question.question}}{{/link-to}}
      <small>asked by: {{question.user}}</small></li>
  {{/each}}
</ul>

